apiVersion: v1
kind: Service
metadata:
  name: student-management-service
spec:
  type: NodePort
  selector:
    app: student-management
  ports:
    - port: 8089          # Port du service
      targetPort: 8089   # Port DANS le pod
      nodePort: 30007    # Port exposé sur Minikube
